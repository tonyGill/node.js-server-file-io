var buf = new Buffer(10); // 10 Ocetets

var buffArr = new Buffer([2,3,4,5]);

var buffStr = new Buffer("Test string");


buff = new Buffer(256);

len = buf.write("Testing");

console.log(buffStr.toString());

buf.toJSON();

buffConcat = Buffer.concat([buffer1, buffer2]);

var result = buffer1.compare(buffer2);

buffer1.cop(buffer2);


resultSlice = buffer.slice(0,9); //Prints index 0 to 9 of string